---
interface Props {
  label?: string;
}

const { label = "脚注" } = Astro.props;
---

<section class="footnotes" aria-labelledby="footnotes-heading">
  <h2 id="footnotes-heading" class="annotation-label">{label}</h2>
  <slot />
</section>
