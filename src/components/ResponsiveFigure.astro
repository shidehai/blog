---
interface Props {
  src: string;
  srcset?: string;
  alt: string;
  width: number;
  height: number;
  caption?: string;
  eager?: boolean;
}

const { src, srcset, alt, width, height, caption, eager = false } = Astro.props;
---

<figure class="responsive-figure">
  <img
    {src}
    srcset={srcset}
    sizes={srcset ? "(max-width: 48rem) 100vw, 48rem" : undefined}
    {alt}
    {width}
    {height}
    loading={eager ? "eager" : "lazy"}
    decoding="async"
  />
  {caption ? <figcaption>{caption}</figcaption> : null}
</figure>
