# 全项目三方审计报告存档（2026-09-24）

三个并行审计代理分别覆盖：部署/容器层、Directus 脚本层、前端/文档一致性。本文件存档全部发现；本任务只处理其中"快修包"6 项，其余按优先级备查。

## 🔴 实际问题

1. **Markdown 存储型 XSS** — `frontend-v2/components/MarkdownContent.tsx:240` `dangerouslySetInnerHTML`；解析器（92-207 行）只转义代码块（24-27 行），标题/段落/表格/列表原样透传 HTML；CSP `script-src 'self' 'unsafe-inline'`（docs/operations/maintenance-security.md:40-41）不拦内联脚本。触发前提：CMS 作者账号沦陷。【未修 · 需方案决策】
2. **首页零文章构建崩溃** — `app/page.tsx:35` `featuredPosts[0] || allPosts[0]!` 空数组时为 undefined，36 行 `featured.id` 抛 TypeError。【本任务修】
3. **seed 防覆盖对 7/11 字段失效** — `directus/seed/editorial.mjs:9-19` SETTINGS_OWNED_FIELDS 含 5 个幽灵字段（footer_note/seo_description/seo_title/site_author/site_description），schema 实际字段见 `directus/schema.yaml:1802-2482`；owner 改过 tagline/biography/footer_text 等仍被判 exact，PATCH 分支静默覆盖 owner 编辑。【未修 · 安全项】
4. **sftp Restic 示例在容器中必然失败** — `.env.example:28`、`ops/env/production.env.example:19`；backup 容器 read_only、无 SSH 私钥/known_hosts 注入（compose.yaml:183/222/247、backup.Dockerfile:11-13）；restic sftp 后端无 StrictHostKeyChecking 选项。fail-closed，freshness 定时器兜底。修复方向：示例改 rest:/s3: 或挂载只读 .ssh。【未修】

## 🟡 值得改进

### 本任务修（快修包）

5. **schema:dump 失败截断 database.sql** — `package.json:20` `cmd1 && cmd2 > directus/database.sql`，pg_dump 中途失败留下半截文件，而它正是 schema:apply 输入。改临时文件 + mv。
6. **directus catch 丢 cause** — `frontend-v2/lib/directus.ts:352-356` catch 后抛新 Error 不带 `cause`，排查信息被吞。
7. **"报告问题"链接指向陌生账号** — `ArticleClient.tsx:157` 硬编码 `github.com/wildalley`，与 `mock.ts:16` profile `github.com/shidehai` 不一致，模板残留。
8. **文档漂移 ×2** — `docs/operations/deployment.md:80-83` 仍称 push main 触发发布（与 publish.yml dispatch-only 门禁矛盾）；`:87-89` Flow 监听清单缺 `categories, tags, series, posts_tags`（实际见 `directus/bootstrap.mjs:613-624`）。
9. **CI 并发组共享** — `publish.yml:14-19` push 与 dispatch 发布同处 `...-production` 组且 cancel-in-progress，push 可取消进行中的部署。拆组。

### 备查（未修）

10. **launch apply 确认 token 绑错目标** — `directus/launch.mjs:173-189` 校验 manifest.targetOrigin 而非实际 DIRECTUS_URL（client.mjs:1,67-69），无 origin 相等断言。
11. **bootstrap license 前置检查位置错误** — `bootstrap.mjs:94` retire + `149-183` identity upsert 之后，`186-192` 才检查 custom_permission_rules_enabled 并 throw，缺 license 留半成品。应提到首个 mutation 前。
12. **build 用户静默跳过** — `bootstrap.mjs:495-496+507-514` token 缺失时 upsertUser 返回 null，708 行仍打印 "bootstrap complete"。至少 warning。
13. **Flow 条件漏事件** — `bootstrap.mjs:688-697` items.delete 的 payload 只含主键、items.update 只含变更字段，删除文件或仅改 title 时 payload.folder 为 undefined 不触发重建，公开站残留旧资产。【需实测确认】
14. **测试把 500 当预期拒绝** — `tests/directus.mjs:94` rejects 白名单含 500，服务端 bug 会让权限断言假通过。
15. **name 回退匹配与自身安全注释矛盾** — `bootstrap.mjs:60-63` 声明只用固定 ID，主 upsert(29-56) 却按 name/email 回退接管同名实体。
16. **ensureFile 按 title 匹配致孤儿文件累积** — `directus/seed/fixtures.mjs:11-17` title 含内容 SHA，换图即新建，旧 publishable 文件永不清理。
17. **基础设施镜像全用可变 tag** — Dockerfile:2、compose.yaml:9/78/132、backup.Dockerfile:1/3、publish.yml:115 trivy，与项目 digest/SHA 固定纪律不一致。
18. **directus 容器无 read_only/cap_drop** — compose.yaml:77-129，硬化弱于栈内基线。
19. **Markdown 解析器不支持链接和图片** — MarkdownContent.tsx:92-207 无 `[text](url)`/`![alt](src)` 规则，CMS 正文链接显示为原始文本；148+151 行有序列表也包成 `<ul>`。
20. **全文进客户端负载** — `app/layout.tsx:35,44` getAllPosts()（含 content 全文）传给 LayoutShell→CommandMenu，搜索只用 title/summary/category/tags；`archives/[slug]/page.tsx:58-65` relatedPosts 同理。
21. **主题闪烁 + 背景不同步** — `app/layout.tsx:38-42` 无预水合主题脚本；`BackgroundCanvas.tsx:138-150` 只挂载时读一次 data-theme 且先于 Navbar 应用主题；切换主题 canvas 不更新。
22. **系列页空系列静默回退全站文章 + 未用 series_order** — `series/[slug]/page.tsx:89,93`、`series/page.tsx:89,94-96`；未用 directus.ts:237-253 已排好的 series.posts。
23. **SideRight 硬编码活数据** — 110 行"运行 128 天"、129-131"更新 2026-08-20"、162-165 农历文本写死；时钟/日历（15,67-69）跨时区有 hydration mismatch 风险。
24. **ActivityHeatmap 月份/区间硬编码** — 39-93 行月份表与 100 行"2025.08 - 2026.08"写死，mock.ts:29-30 窗口相对今天滚动，过后必错位。
25. **tools 页 link 死字段 + 假交互** — `app/tools/page.tsx:23,33-74,160`。
26. **首页分页纯装饰** — `app/page.tsx:74-103`。
27. **主题菜单缺"跟随系统"项** — `Navbar.tsx:197-239`，selectTheme 支持 system 但 UI 无入口。
28. **SEO 缺口** — 无 sitemap.ts/robots.ts/OG/canonical；`layout.tsx:19-28` 无 metadataBase。

## ⚪ 吹毛求疵（略）

deriveExcerpt 正则无行首锚点（"C# 语言"被误吃）、wordCount 含语法符号、`/` 快捷键未排除 contenteditable、scroll 未 passive、TagsClient 双 aria-current、Cmd+K 双重注册、CommandMenu 遮罩无点击关闭、fixture 死字段（default_seo_description 等）、Navbar 搜索输入被丢弃、snapshot.check 断言 3 篇依赖 env 干净、next/font/google 构建期联网、.dockerignore .env 规则重复、dev override 网络放宽、CMS CSP 仅 frame-src、dev command 与 Dockerfile CMD 双写、SIGKILL 残留 dump 窗口（umask 077 已缓解）、email 进错误日志、TFA 测试耦合未文档化、legacy 数据无预检。

## ✅ 审计过无发现

构建期密钥面、备份验证链（pg_restore --list + sha256 + restore 二次校验 + RESTORE_CONFIRM）、健康检查与卷挂载一致性、deploy-site.sh 与文档、CI secrets 判空/digest 校验/Trivy 阻断、错误处理与幂等性（bootstrap/seed/launch）、凭据不回显、确认 token 本体（除 #10）、README 命令与 package.json 全部一致、env.ts 凭据边界、/healthz 与 /writing 308 契约、fixture/types 与解码 schema 对齐。
